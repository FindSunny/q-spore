'use strict';

/**
 * RabbitMQ 模块
 * 
 * 含有RabbitMQ队列服务生产,消费等功能
 * 
 * @module rabbitMq
 */

const rabbitMq = {
    // 打印信息
    log: function (msg) {
        console.log(msg);
    }
}

module.exports = rabbitMq;